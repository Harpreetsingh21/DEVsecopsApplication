version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=devsecopsapplication -Dsonar.organization=devsecopsapplication -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=0e5dfb71222308aa5b312a56526203c8ca3f1225
